# ---------------------------------------------------------------------------
# Sentinel-AI — dependencies
# Python 3.11+ (tested on 3.13)
# ---------------------------------------------------------------------------

# --- LLM engine -------------------------------------------------------------
anthropic>=0.40.0

# --- Agent orchestration ----------------------------------------------------
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.0

# --- Document processing ----------------------------------------------------
pymupdf>=1.24.0            # primary PDF extraction (text + tables + coordinates)
pdfplumber>=0.11.0         # fallback parser for stubborn table layouts

# --- Deterministic verification layer --------------------------------------
pandas>=2.2.0
numpy>=1.26.0

# --- Retrieval --------------------------------------------------------------
llama-index-core>=0.11.0
llama-index-vector-stores-chroma>=0.3.0
llama-index-embeddings-huggingface>=0.3.0
chromadb>=0.5.0
# Upgrade path: swap chromadb -> PostgreSQL + pgvector
# (llama-index-vector-stores-postgres) once the corpus exceeds a single host.

# --- Web search -------------------------------------------------------------
tavily-python>=0.5.0
httpx>=0.27.0

# --- Scheduler --------------------------------------------------------------
apscheduler>=3.10.0

# --- Frontend ---------------------------------------------------------------
streamlit>=1.50.0
altair>=5.4.0

# --- Common -----------------------------------------------------------------
pydantic>=2.8.0
python-dotenv>=1.0.0

# --- Tests ------------------------------------------------------------------
pytest>=8.0.0
